rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Allow read/write to all authenticated users during testing
      allow read, write: if true;
    }
    
    match /{document=**} {
      allow read, write: if false;
    }
  }
} 